export interface AppData {
    name: string;
    icon: string;
    url: string;
    id: number;
}

export enum Apps {
    CLIPPER = "Clipper",
    SCRAPBOOKER = "Scrapbooker",
    PODCLIP = "Podclip"
}